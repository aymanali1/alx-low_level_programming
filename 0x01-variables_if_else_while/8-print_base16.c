#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int i = 48;

	for (i = 48; i <= 57; i++)
	{
		putchar(i);
	}

	for (i = 65; i <= 70; i++)
	{
		putchar (i);
	}
	putchar('\n');

	return (0);
}
